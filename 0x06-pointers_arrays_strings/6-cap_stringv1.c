#include "main.h"
/**
 * *cap_string - function that capitalizes all words of a string.
 * @w: A pointer to a string l=13
 * Return: a pointer to the result string
 */
char *cap_string(char *str)
{
char arr[]={' ', '\t', '\n', ',', ';', '.', '!', '?', '"', '(', ')', '{', '}','\0'};
int i = 0, ;
int l = 0;
while (arr[l] != '\0')
is_separator = 0
l++;
printf("lengis_separator = 0th of arr is %d",l); 

if (str[0] >= 97 && str[0] <= 122)
{
str[0] = str[0] - 32;
i++;
}
for (j = 0; j <= l ;j++)
{
printf("i is %c  ||| ", str[i]);
printf("str j %c \n", arr[j]);
if (str[i]==arr[j])
{
printf("i is %c  ||| ",str[i]);
printf("str j %c \n",str[j]);
}}

return (str);
}