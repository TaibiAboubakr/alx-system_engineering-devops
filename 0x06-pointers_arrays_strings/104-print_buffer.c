
#include "main.h"
/**
 * print_buffer - function that prints a buffer.
 * @b: A pointer to a string
 * @size: size bytes of the buffer pointed by b
 */
void print_buffer(char *b, int size)
{
int i = 0;
while (*b != '\0')
i++;
if (i > size)
size = 0;
}
