#include "main.h"
/**
 * mul  - multiplies two integers.
 * @a : integers to multiple
 * @b : integers to multiple
 * Return: multiplie of two integers .
 *
 */
int mul(int a, int b)
{
int m;
m = a * b;
return (m);
}
